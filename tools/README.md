# Tools

Python scripts for automated tasks go here.

Follow WAT Layer 3 principles:
- Each script does ONE thing deterministically
- Reads inputs from args or environment — never hardcoded
- Logs output clearly
- Credentials from .env only — never in the script

## Current Tools
(none yet — add as the project grows)

## Example future tools
- `export_inquiries.py` — export vendor_inquiries table to CSV
- `seed_demo_data.py` — seed demo vendors/products for local dev
